# Handoff to Claude — (not yet written this session)

Hermes: overwrite this whole file as the last commit of your session, using
the format defined in `HANDOFF.md`. Don't append to this — replace it.

Minimum required sections: commit hash, what you actually tested and how,
pass/fail against Claude's "Your job" list from `HANDOFF_TO_HERMES.md` with
evidence (not just verdicts), any new issues found with repro steps, and
anything you need Claude to decide or fix next.
